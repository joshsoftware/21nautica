= link_to "Add New Vendor Invoice", new_bill_path, class: 'btn btn-primary pull-right'
%br
%br

%table.table#bills
  %thead
    %th.hide
    %th Bill Number
    %th Bill Date
    %th Vendor 
    %th Value
    %th.remark Remarks
    %th Created By
    %th Approved By
    %th Action
  %tbody
    - @bills.each do |bill|
      %tr
        %td.hide= bill.created_at
        %td= bill.bill_number
        %td= bill.bill_date
        %td= bill.vendor.try(:name)
        %td= bill.value
        %td.remark
          = bill.remark
          - if bill.debit_notes.present?
            |
            = bill.debit_notes.collect(&:reason).join(',') if bill.debit_notes.where.not(reason: [nil, ''])
        %td= bill.created_by.try(:email)
        %td= bill.approved_by.try(:email)
        %td= link_to 'Edit', edit_bill_path(bill), class: 'btn btn-primary' 
